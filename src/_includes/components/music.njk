<div class="music-block">
  <span class="music-label">▶ NOW PLAYING</span>
  <span class="music-sep">│</span>
  <span class="music-artist">{{ music.artist }}</span>
  <span class="music-sep">–</span>
  <a href="{{ music.url | safe }}" target="_blank" rel="noopener noreferrer">{{ music.title }}</a>
</div>